name: Emergency login repair
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Emergency login repair workflow is intentionally disabled after final dashboard verification."
