// Dashboard analytics module — Supabase-backed
(async()=>{const S=window.supabaseClient||window.db;if(!S)return;const fmt=n=>Number(n||0).toLocaleString();const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let q=await S.from('daily_inspections').select('inspection_date,customer,part_no,part_name,lot_no,in_qty,test_qty,bad_qty,result,defect_content,inspector').order('inspection_date',{ascending:false});if(q.error)return;const rows=q.data||[];const host=document.getElementById('dashAnalytics');if(!host)return;const days=[...Array(7)].map((_,i)=>{const d=new Date();d.setDate(d.getDate()-i);return new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Ho_Chi_Minh'}).format(d)}).reverse();const daily=days.map(d=>{const a=rows.filter(r=>r.inspection_date===d);return {d,in:a.reduce((s,r)=>s+(r.in_qty||0),0),test:a.reduce((s,r)=>s+(r.test_qty||0),0),ng:a.reduce((s,r)=>s+(r.bad_qty||0),0)}});const maker={};rows.forEach(r=>{const m=r.customer||'Unknown';maker[m]??={cnt:0,test:0,ng:0};maker[m].cnt++;maker[m].test+=r.test_qty||0;maker[m].ng+=r.bad_qty||0});const topM=Object.entries(maker).sort((a,b)=>b[1].ng-a[1].ng).slice(0,8);const defects={};rows.filter(r=>r.bad_qty>0).forEach(r=>{const d=r.defect_content||'미지정';defects[d]=(defects[d]||0)+(r.bad_qty||0)});const topD=Object.entries(defects).sort((a,b)=>b[1]-a[1]).slice(0,10);host.innerHTML=`<div class="grid"><div class="panel"><h3>최근 7일 검사 추이</h3><div style="display:grid;grid-template-columns:repeat(7,1fr);gap:8px;align-items:end;height:170px">${daily.map(x=>`<div style="text-align:center"><div title="검사 ${fmt(x.test)} / NG ${fmt(x.ng)}" style="height:${Math.max(8,Math.min(140,(x.test||1)/Math.max(...daily.map(z=>z.test),1)*140))}px;background:#2563eb;border-radius:5px 5px 0 0"></div><small>${x.d.slice(5)}</small></div>`).join('')}</div></div><div class="panel"><h3>Maker별 불량률</h3>${topM.map(([m,x])=>`<div class="mini"><span>${esc(m)}</span><b>${x.test?(x.ng/x.test*100).toFixed(2):'0.00'}% · ${x.cnt}건</b></div>`).join('')||'<p>데이터 없음</p>'}</div></div><div class="grid"><div class="panel"><h3>FAIL / 불량 Top 10</h3>${topD.map(([d,n],i)=>`<div class="mini"><span>${i+1}. ${esc(d)}</span><b>${fmt(n)} PCS</b></div>`).join('')||'<p>불량 없음</p>'}</div><div class="panel"><h3>전체 DB 현황</h3><div class="mini"><span>검사 건수</span><b>${fmt(rows.length)}</b></div><div class="mini"><span>검사 수량</span><b>${fmt(rows.reduce((s,r)=>s+(r.test_qty||0),0))}</b></div><div class="mini"><span>NG 수량</span><b>${fmt(rows.reduce((s,r)=>s+(r.bad_qty||0),0))}</b></div></div></div>`})().catch(console.error);